<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.yuriydazhuk.rxexample"
    android:versionCode="1"
    android:versionName="Rx">

    <uses-sdk
        android:minSdkVersion="16"
        android:targetSdkVersion="16" />

    <uses-permission android:name="android.permission.INTERNET" />

    <application android:label="Rx">
        <activity android:name=".MainActivity" android:label="Rx">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data android:name="roboguice.modules"
            android:value="com.example.yuriydazhuk.rxexample.BindingModule" />
    </application>
</manifest>
